<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="seoul.tour.mapper.FreeBoardMapper">

<select id="getList" resultType="seoul.tour.domain.FreeBoardVO">
<![CDATA[
select * from tbl_freeboard where bno > 0
]]>
</select>

<insert id="insert">
	insert into tbl_freeboard (bno, title, content, writer)
	values (seq_board.nextval, #{title}, #{content}, #{writer})
</insert>

<insert id="insertSelectKey">
	<selectKey keyProperty="bno" order="BEFORE"
	resultType="long">
	select seq_board.nextval from dual
	</selectKey>
		insert into tbl_freeboard (bno, title, content, writer)
	values (#{bno}, #{title}, #{content}, #{writer})
</insert>
	
<select id="read" resultType="seoul.tour.domain.FreeBoardVO">
	select * from tbl_freeboard where bno = #{bno}
</select>

<delete id ="delete" >
	delete tbl_freeboard where bno = #{bno}
</delete>

<update id="update">
	update tbl_freeboard
	set title= #{title},
	content=#{content},
	writer = #{writer},
	modified_date = sysdate
	where bno = #{bno}
</update>


</mapper>